# Immediate Next Actions

1. Merge the judge-ready release-gate PR after CI passes.
2. Upgrade vulnerable dependencies in a separate tested PR.
3. Deploy the public deterministic demo.
4. Test GPT-5.6 mode on the deployed instance.
5. Capture screenshots and record the demo video.
6. Run `/feedback` in the primary Codex build session.
